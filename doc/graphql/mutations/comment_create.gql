        <<~GQL
            mutation($text: String!, $postId: ID!){
            createComment(input: {text: $text, postId: $postId}){
              clientMutationId
              status
            }
          }
        GQL